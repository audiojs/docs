{
  "name": "docs",
  "description": "Documentation for audiojs packages",
  "version": "0.0.0",
  "homepage": "https://audiojs.github.io/docs",
  "author": "Jamen Marz (https://git.io/jamen)",
  "repository": "audiojs/docs",
  "license": "MIT",
  "private": "true",
  "bugs": {
    "url": "https://github.com/audiojs/docs/issues"
  },
  "keywords": [
    "audiojs",
    "docs"
  ],
  "scripts": {
    "build": "npm run clean && node deploy/build",
    "deploy": "npm run build && node deploy && npm run clean",
    "clean:cache": "rm -rf deploy/cache",
    "clean": "rm -rf dist deploy/tmp"
  },
  "devDependencies": {
    "cpy": "^5.0.0",
    "ecodoc": "^0.0.0",
    "es2040": "^1.2.4",
    "gh-pages": "^0.12.0",
    "pull-browserify": "^0.2.2",
    "pull-stream": "^3.5.0",
    "pull-vinyl": "^1.2.0"
  },
  "dependencies": {
    "hyperapp": "^0.7.1",
    "marked": "^0.3.6",
    "sheetify": "^6.0.1"
  }
}
